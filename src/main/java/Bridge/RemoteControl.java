package Bridge;

/**
 * Interface representing a remote control for a car.
 * Defines methods for controlling the car’s movements.
 */
public interface RemoteControl {
    void forward();
    void backward();
    void left();
    void right();
}
